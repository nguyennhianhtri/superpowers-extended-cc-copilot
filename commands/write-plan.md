---
description: Create a detailed implementation plan with bite-sized, dependency-tracked tasks.
---

Use the `skill` tool to invoke the **writing-plans** skill and follow it exactly as presented to you. Create the tasks with the `sql` tool on the `todos` + `todo_deps` tables (see using-superpowers/references/task-management.md).
